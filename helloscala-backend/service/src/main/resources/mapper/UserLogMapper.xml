<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.helloscala.common.mapper.UserLogMapper">
    <select id="countUserIP" resultType="com.helloscala.service.web.view.UserIPCountView">
        select count(distinct ip) as ip,count(ip) as count,DATE_FORMAT(create_time,'%Y-%m-%d') createDate from b_user_log where DATE_FORMAT(create_time,'%Y-%m-%d')&gt;=#{date}  GROUP BY DATE_FORMAT(create_time,'%Y-%m-%d')
    </select>
</mapper>
